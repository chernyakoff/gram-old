"""
Project-local utilities package.

This file exists primarily to make `utils.*` imports unambiguous for type checkers
and IDEs (avoid resolving to any third-party `utils` package on the interpreter
path).
"""

