name: neurogram-dev

services:
  postgres:
    image: pgvector/pgvector:pg15
    container_name: neuro-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: neurogram
    ports:
      - "5445:5432" # доступ с хоста (ноута)
    volumes:
      - postgres-data-dev:/var/lib/postgresql/data
    networks:
      - dev

  base:
    profiles: ["workers"]
    build:
      context: ../..
      dockerfile: app/workers/base/Dockerfile
      target: dev
    container_name: neuro-base
    volumes:
      - ../../app:/app
      - uv-venv-base:/app/.venv # отдельный том под venv worker
      - ./config.yml:/app/config.yml
    environment:
      - UV_SYSTEM_PYTHON=1
      - PYTHONPATH=/app
      - UV_LINK_MODE=copy
    command: ["uv", "run", "python", "-m", "workers.base"]
    depends_on:
      - postgres
    networks:
      - hatchet
      - dev
      - web

  dialog:
    profiles: ["workers"]
    build:
      context: ../..
      dockerfile: app/workers/dialog/Dockerfile
      target: dev
    container_name: neuro-dialog
    volumes:
      - ../../app:/app
      - uv-venv-dialog:/app/.venv # отдельный том под venv worker
      - ./config.yml:/app/config.yml
    environment:
      - UV_SYSTEM_PYTHON=1
      - PYTHONPATH=/app
      - UV_LINK_MODE=copy
    command: ["uv", "run", "python", "-m", "workers.dialog"]
    depends_on:
      - postgres
    networks:
      - hatchet
      - dev
      - web

  api:
    build:
      context: ../..
      dockerfile: app/api/Dockerfile
      target: dev
    container_name: neuro-api
    ports:
      - "8833:8833" # маппим наружу API
    volumes:
      - ../../app:/app
      - uv-venv-api:/app/.venv # отдельный том под venv api
      - ./config.yml:/app/config.yml
    environment:
      - UV_SYSTEM_PYTHON=1
      - PYTHONPATH=/app
    extra_hosts:
      - "host.docker.internal:host-gateway"
    command: ["uv", "run", "python", "-m", "api.dev"]
    depends_on:
      - postgres
    networks:
      - hatchet
      - dev
      - web

  web:
    build:
      context: ../../web
      target: dev
    container_name: neuro-web
    ports:
      - "5173:5173"
    volumes:
      - ../../web:/app
      - /app/node_modules
      - ./config.yml:/app/config.yml
    depends_on:
      - api
    networks:
      - dev
      - web

  minio:
    build:
      context: ./minio
    container_name: neuro-minio
    environment:
      MINIO_ROOT_USER: mike
      MINIO_ROOT_PASSWORD: gabun8374
      S3_ACCESS_KEY: "IhDRQQH1TfL0wQtUDpKV"
      S3_SECRET_KEY: "G2J10kVTv3E6eBMHOcmnQIrM00fqwa3fFpqCs49g"

    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio-data:/data
    networks:
      - dev
      - web

volumes:
  uv-venv-base: {}
  uv-venv-dialog: {}
  uv-venv-api: {}
  postgres-data-dev: {}
  minio-data: {}

networks:
  hatchet:
    external: true
    name: hatchet-network
  web:
    driver: bridge
  dev:
    driver: bridge
